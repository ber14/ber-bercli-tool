[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "berber"
version = "0.1.0"
description = "cli"
requires-python = ">=3.8"

[project.scripts]
ber = "tool.main:main"
